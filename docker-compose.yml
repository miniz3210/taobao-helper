version: '3.8'

services:
  taobao-helper:
    build: .
    container_name: taobao-helper
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./app:/app/app
    env_file:
      - /env/.env
    restart: unless-stopped
